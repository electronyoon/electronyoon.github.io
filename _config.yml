title: 윤전자의 블로그
author: electronyoon
email: electronyoon@gmail.com
url: https://electronyoon.github.io
baseurl: ""
description: >
  윤전자의 블로그입니다.

permalink: /:slug.html

lang: ko # 사이트 기본 언어

# 발행일을 한국 시각으로 해석한다.
# 없으면 빌드 컨테이너(UTC) 기준이 되어, 발행일 당일 글이 미래로 취급돼 빌드에서 빠진다.
timezone: Asia/Seoul

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics
google_analytics: "G-PCLZ77RFML"

theme: no-style-please # if you are using GitHub Pages, change it to remote_theme: riggraz/no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: ".." # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: false # show blog description in home page

sass:
  style: :compressed

# 다국어: 모든 글은 lang: ko 로 두고, 번역본만 lang: en / lang: zh 로 덮어쓴다.
# 원문-번역본은 같은 ref 값으로 묶는다(예: ref: three-clocks). ref 는 언어 전환·hreflang 에 쓰인다.
defaults:
  - scope:
      path: ""
      type: posts
    values:
      lang: ko

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

exclude:
  - .donotcommit
